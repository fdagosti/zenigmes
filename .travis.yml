language: node_js
node_js:
  - "5"

services: mongodb

env: 
  - JWT_SECRET=foo
  - TRAVIS=true

before_install:
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

